import { styled } from '@material-ui/core';
import React from 'react';
const RowWrapper = styled('div')({
    marginBottom: '1rem'
});

export default function HomeSettingsTab({ settings, groupBy }) {
    return (
        <>
        <h1>Home Settings</h1>
        </>
    )
}